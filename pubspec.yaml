name: sannote
description: "산노트 - 수도권 등산 초보를 위한 입구·코스 단위 등산 코스 앱"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.1
  flutter_naver_map: ^1.4.4
  flutter_riverpod: ^3.4.3
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env

